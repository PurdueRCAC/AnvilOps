# sample

This is a sample app used to test AnvilOps's deployment capabilities. It can be built with the included `Dockerfile` or with Railpack.

It's an HTTP server that responds to requests on port 8080 with a static message.
