{{ if and .Values.buildkitd.install .Values.buildkitd.netpol .Values.buildkitd.netpol.buildctlIngress -}}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: anvilops-buildctl-ingress
  labels:
    {{- include "anvilops.commonLabels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "anvilops.buildkitd.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
  egress:
    {{- toYaml .Values.buildkitd.netpol.buildctlIngress | nindent 4 }}
{{- end }}